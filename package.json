{
  "name": "mern-portfolio",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
  "build": "cd client && npm install --no-audit && npm run build"
  },
  "engines": {
    "node": "18.x"
  },
  "author": "Dependra",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^17.2.1",
    "express": "^5.1.0",
    "nodemailer": "^7.0.5",
    "nodemailer-sendgrid-transport": "^0.2.0"
  }
}
